this is a REST API server
routers supports the REST API endpoints for the slash dishes

express application work is that the middleware

--morgan: change color output in console

type: express conFusionServer to create folder conFusionServer
then cd into it and type install npm to add node_modules and independencies in packet.json

--
install mongoose bluebird mongoose-currency --save
    mongoose currency use for "cost" in dishes

--setting admin account
access directly to db and type: db.users.update({"username": "..."}, {$set: {"admin":true}})